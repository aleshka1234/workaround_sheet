[project]
name = "workaround-list"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.18.4",
    "django>=6.0.3",
    "psycopg>=3.3.3",
    "psycopg2-binary>=2.9.11",
    "sqlalchemy>=2.0.49",
]
